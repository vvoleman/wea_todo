export default {
    "form": {
        "rules": {
            "required": "Required field",
            "min": "Minimum number of characters is {min}",
            "max": "Maximum number of characters is {max}",
            "email": "Invalid email format"
        }
    }
}